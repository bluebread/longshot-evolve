# pytest.ini
[pytest]
filterwarnings =
    ignore::DeprecationWarning:pkg_resources
    ignore::DeprecationWarning:binarytree
    ignore::DeprecationWarning:sphinxcontrib
markers =
    integration: marks tests as integration tests (deselect with '-m "not integration"')
